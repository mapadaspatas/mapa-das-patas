# Imagens de marca para as redes

Geradas por `scripts/build-social.ts` (`pnpm build:social`) a partir da pata-Brasil,
do mapa de UFs e da cópia da home — os mesmos arquivos que o site usa. Não editar
à mão: rode o script de novo depois de mexer no logo, no mapa ou em `strings.ts`.

| Arquivo | Onde vai | Tamanho |
| --- | --- | --- |
| `instagram-perfil.png` | Foto de perfil do @mapadaspatas | 1080×1080 (exibida em círculo) |
| `instagram-post.png` | Post do feed | 1080×1080 |
